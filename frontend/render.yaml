services:
  - type: web
    name: content-moderation-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    envVars:
      - key: REACT_APP_NODE_ENV
        value: production
      - key: REACT_APP_API_URL
        value: "https://ai-powered-content-moderation-web-app.onrender.com" # Your actual backend URL
    routes:
      - type: rewrite
        source: /*
        destination: /index.html